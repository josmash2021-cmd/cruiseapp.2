# AGENTS.md — Cerebro de navegación Kimi · CruiseApp

> **Propósito:** trabajar con el mapa ya conocido, SIN gastar tokens explorando.
> Este archivo se carga solo en cada sesión — cada línea cuesta tokens siempre, así que es denso a propósito.
> **Regla de oro:** si el flujo está en el mapa de abajo, ve DIRECTO a los archivos listados. No lances agentes de exploración ni leas carpetas enteras para territorio ya mapeado.

---

## ⚡ Workflow token-eficiente

1. Lee la tarea → ubica el flujo en el mapa → lee SOLO los archivos/funciones listados (`Read` con `line_offset`, no archivos completos de 3,000 líneas).
2. Agente `explore`: SOLO para flujos NO mapeados aquí. Nunca para "confirmar" lo que este mapa ya dice.
3. El mapa dice DÓNDE; antes de editar lee igual la región exacta (los números de línea se mueven con cada commit). La forma de trabajar NO cambia: diffs mínimos, estilo del archivo, verificación obligatoria.
4. Verificación tras editar: `flutter analyze` (Dart) · `cd backend && ./.venv/Scripts/python.exe -m pytest tests/ -q` (Python).
5. `CLAUDE.md` (biblia: stack, trip lifecycle, 29 patrones aprendidos) y `PROJECT_MAP.md` (estatus 🔴/🟡/🟢 de cada archivo): léelos SOLO si el flujo no está mapeado aquí o dudas del estatus de un archivo raro. Ya no son lectura obligatoria de cada sesión.
6. Antes de commitear: `git status` — el usuario suele tener WIP propio sin commitear; no arrastres archivos ajenos al commit (si no hay forma, decláralo en el mensaje). Nunca commitees `.claude/scheduled_tasks.lock`. Mensajes en español, estilo conventional (`fix(fcm): ...`). Push al remote después de cada change set (preferencia durable del usuario).

---

## 📏 Contrato de comportamiento — TODA sesión (2026-09-11, spec del usuario)

1. **Verifica que NO existe antes de proponer construirlo.** Grep primero, planear después. (2026-09-11: se entró a plan-mode a "crear" el gate del botón Select que ya existía en `ride_request_widgets.dart` — pérdida de tiempo.)
2. **Pregunta antes de actuar ante cualquier ambigüedad, cambio grande o algo fuera de lo pedido.** No autonomía: el usuario decide; tú ejecutas lo decidido. Si la orden es clara y chica, hazla sin preguntar.
3. **Nada roto se entrega:** tras editar corre `flutter analyze` (Dart) / `pytest tests/ -q` (Python) COMPLETOS — no solo el test que tocaste. Si tu cambio rompe otra cosa, arreglarla es parte de la tarea. Declara en el mensaje final lo que NO pudiste verificar.
4. **Diff mínimo y limpio:** estilo del archivo, sin comentarios ruido, sin archivos/reportes no pedidos, no tocar lo que no requiere la tarea.
5. **Cero vulnerabilidades nuevas:** sin `catch (_) {}` silenciosos, sin secretos hardcodeados, todo endpoint nuevo con `_verify_api_key` + auth, inputs con Pydantic, SQL con ORM/`text()` parametrizado.
6. **Cero regresiones de diseño/UI:** si tocas una pantalla, verifica estados hermanos (botón on/off, loading, error, vacío, ES/EN) — no solo el caso feliz.
7. **Si dudas de algo que el usuario afirma, créeselo primero y rastrea el código; con evidencia en mano, dilo y acata su decisión.**

---

## 🧠 Mapa de flujos — dónde está cada cosa

**Push de oferta al driver (Issue 1, arreglado 2026-08-07; endurecido 2026-08-08)**
- Backend: `backend/routers/dispatch.py` → `_send_offer_to_driver` (~L575-800): crea DispatchOffer, re-verifica `is_online` antes de enviar, SSE + FCM con body `"$ fare · $/hr · mi · min"`. `_auto_cascade` (~L751): OFFER_TIMEOUT_SECONDS = **20 s** (`config.py:44`) × máx 10 drivers. El FCM solo se suprime si hay token LA **y** `apns_configured()` (sin credenciales APNs, el banner sale igual). `accept_offer` ya limpia la isla (`_clear_live_activity_offer`), igual que reject/expire. `get_driver_pending` exige `user.id == driver_id` (403 como el SSE).
- Payload FCM/APNs: `backend/services/fcm_service.py` → `_send_fcm_push` (~L236-372): canales `cruise_offers` (max) / `cruise_premium`, sonido `cruise_online`, TTL 45 s, APNs time-sensitive para ofertas. Test guardián: `backend/tests/test_fcm_message_builds.py`.
- App: `lib/main.dart` — bg handler (~L133-219: ofertas retornan temprano, las dibuja el OS), foreground (~L922-1045), tap routing (`_handleDriverRideOffer` ~L222). **Tap = tarjeta INSTANTÁNEA (2026-08-09):** `push_data` en `dispatch.py`/`guardian_agent.py` lleva TODOS los campos de la tarjeta (rider, coords, direcciones, earnings, timeout) → `_provisionalOfferFromPush` la dibuja sin red y `_fetchPendingOffer` solo reconcilia después (full offer reemplaza; "reachable y no está" → `DriverOnlineScreen.removeOfferNotifier` la baja + toast; jamás si hay accept en vuelo). Payloads viejos (solo ids) caen al flujo lookup-first. `lib/services/notification_service.dart` — canales (~L278-329), `showOfferNotification` (~L453-518, fullScreenIntent, timeSensitive).
- Token: `NotificationService.ensureTokenRegistered` → `POST /auth/fcm-token`. SSE en vivo: `ApiService.streamDriverOffers` → `driver_online_controller._applyOffers` (los handlers SSE/poll pasan la lista AUNQUE VACÍA a `_applyOffers` — si no, la oferta muerta se queda pintada; guardián: `test/offer_live_activity_guard_test.dart`). Resume fuerza `_startPolling(force: true)`. **SSE NO manda snapshot al suscribir** y el timer de poll se apaga con `_sseActive` → una oferta cortada con el socket muerto (iOS bg/killed) nunca se re-entregaba y el tap del push caía en "Finding trips" eterno (fix 2026-08-09: `_startPolling` hace un `_poll()` one-shot en cada (re)start + el lookup tardío del tap se inyecta por `deepLinkOfferNotifier` en vez de descartarse). `registerLiveActivityToken` reintenta 1× a los 10 s.
- iOS Live Activity: `ios/Runner/AppDelegate.swift` `CruiseLiveActivityManager` + `lib/services/live_activity_service.dart`; arranca al ir online (`_showOnlinePresence`), ofertas por `_syncOfferLiveActivity` y por push APNs `liveactivity` (`backend/services/apns_liveactivity.py`, `httpx[http2]` en requirements — sin el extra muere en silencio en redeploy). Push-to-start SÍ implementado (iOS 17.2+, `pushToStartTokenUpdates` kind `push_to_start` → `event:"start"` backend): con la app muerta la isla arranca sola si el start-token llegó al servidor. **Fix registro de tokens (2026-08-22):** iOS emitía el push-to-start al arrancar, ANTES de que Dart instalara su handler → se perdía y solo re-emite al rotar (prod: TODOS los drivers con tokens LA NULL → background solo recibía banner FCM). Nativo cachea cada token (`latestTokens` en `CruiseLiveActivityManager`) y los re-emite cuando Dart avisa `pushTokenHookReady` (lo llama `installPushTokenHook`); `start()` además re-lee `Activity.pushToStartToken` en cada online. Si el push de la isla NO aterriza (canal muerto o error transitorio de APNs), `_send_live_activity_offer` manda el banner FCM de respaldo para ESA misma oferta (spec 2026-08-22 "no debe fallar") — la isla solo sustituye al banner cuando el push cae. Guardianes backend: `backend/tests/test_live_activity_offers.py` (payload APNs, stale 400/410, fallback FCM, 403 pending).
- **Driver offline NO recibe ofertas (2026-08-16):** el flip `is_online` bypasea el throttle de 3 s del PATCH location (antes se tragaba el offline); `/auth/me` y `/auth/dashboard` ya NO ponen `is_online=True` a drivers (solo riders — el driver pasa online SOLO por el toggle); irse offline expira las DispatchOffer pendientes (`expire_pending_offers_for_driver` en dispatch.py, llamado desde drivers.py y `/auth/offline`); scheduled dispatcher (main.py) y web dispatch (payments.py) exigen `last_active_at` <15 min como el dispatch en vivo. App: `_wentOffline` (driver_online) frena el heartbeat 30 s que re-escribía online tras el offline. Guardián: `backend/tests/test_offline_offer_guard.py`.

**Movimiento fluido en mapas (2026-08-16)** — motor único `lib/utils/smooth_motion.dart` (velocidad constante + corrección proporcional, tick por vsync): velocidad medida por **timestamp del fix** (param `timestampMs` — la llegada en ráfagas del socket leía pulsos acelera-frena), extrapolación 5 s, holds 2 máx, bleed 0.85, bearing del GPS solo si el fix muestra ≥1 m/s (el temblor parado lo cura `setBearing` por brújula). Radio del hold pineado en 15 m por `test/smooth_motion_bearing_test.dart` — NO bajarlo. **Sprint-y-parqueo curado (2026-08-16, fix definitivo):** la corrección apunta al target **EXTRAPOLADO** (`target + v*edad_del_fix`, lead cap `_maxLeadSec` 2 s), no al fix crudo — antes el marcador sprintaba al fix viejo, el clamp lo frenaba en seco y se parqueaba hasta el siguiente fix (33-73% de frames quietos a 8 m/s constante; ahora 0%). Tope de aceleración `_maxAccelMps2` 8 m/s² (el primer delta tras un alto medía 40 m/s fantasmas). Guardián: `test/smooth_motion_glide_test.dart` (si falla, volvió el paso-a-cada-segundo). **Relay rider:** el socket/RTDB llevan `captured_at` = `Position.timestamp` del fix (gps_service/socket_service); el rider lo prefiere sobre `timestamp` (hora de envío) y el dedup por ts idéntico tira los re-envíos del mismo fix que medían 0 m/s y envenenaban la velocidad. Rider tracking: el carro gira con la **tangente de la ruta** (`_posAtDistUltraSmooth`, look-ahead 18 m, lateral <30 m) en vez del heading GPS crudo. Home rider mini mapa: `riderLocationSettings()` en `lib/utils/driver_location_settings.dart` — un `LocationSettings` pelado en Android da 1 fix cada 5 s (default nativo) y el dot saltaba. **Stationary freeze (2026-09-17, "el carro se aleja/acerca estando estacionado"):** `setTarget` acepta `speedMps` = la velocidad que REPORTA el fix (`Position.speed` o el `speed` del relay socket/RTDB); lectura válida <0.9 m/s congela el marcador (target quieto + velocidad 0, sin tocar holds) y ≥1.2 lo libera — antes un salto de 30-60 m por mala señal medía 30-60 m/s de velocidad implícita y el marcador se iba tras él (y la regla de acuerdo de los holds forzaba cada 3er fix errante). Fixes sin lectura (speedMps null, iOS -1) caen al camino viejo (hold por accuracy) — el freeze es opt-in por fix. Call sites con speed: nav view, rider tracking, minimapa del trip, online controller (`_smoothMoveTo`), Find-My (`_acceptDriverFix` lee `data['speed']`). Guardián: `test/smooth_motion_stationary_test.dart`.

**Mapa rider — pedir viaje / picker pin-drop (Issues 2 y 4, arreglados 2026-08-07)**
- `lib/screens/ride_request_{screen,controller,map,widgets}.dart` = UNA sola State class vía part files.
- Estado: `lib/state/rider_trip_controller.dart` (`RiderPhase`; `_tryFetchRoute` CONSERVA `pickingLocation`; la salida del picker es SOLO por `finishPickingLocation()`).
- Picker "Set your drop-off" = `RideRequestScreen(pickerMode: true)` empujado desde `pickup_dropoff_search_screen.dart`. Latch anti-snap `_userTookCamera`; gate GPS `_gpsMayMoveCamera` (incluye `_mapMounted` desde 2026-08-09 — una sheet tapada NO mueve cámara). **Boot build 575 (2026-08-09):** en picker mode `_center` se siembra con el handoff (la predicción elegida), no con el pickup/GPS; `cameraOptions` fuerza `widget.handoffLat/Lng` y `_gpsMayMoveCamera` hace early-return — el mapa abre SIEMPRE en la dirección seleccionada, no en la ubicación en tiempo real. Reverse-geocode desde el CENTRO del mapa (`_pickerOnCameraIdle`). Si el MapWidget se recrea (GPU/handoff), bootea desde `_lastCam*` — sembrado en initState desde el handoff — NUNCA del GPS. **`_mapSurfaceOwner` es POR INSTANCIA** (`RideRequest-N`, 2026-08-09): sheet y picker son AMBAS RideRequestScreen y el picker va encima de la sheet — con el id estático compartido el coordinator saltaba el revoke (`_owner == owner`), la sheet de abajo quedaba viva y su `_initLocation` tardío (fix GPS frío, hasta 10 s) volaba la cámara a GPS top-down mientras el rider arrastraba: el snap-back de "recién abro la app". Test guardián: `test/picker_camera_guard_test.dart` (si falla, el snap-back o el boot en GPS volvieron).
- "Driver Found": `_dfFlyMainCamera` encuadra la ruta COMPLETA una vez (pitch 20), nunca zoom al punto medio.

**Tracking rider (viaje en curso)**
- `lib/screens/rider_tracking_screen.dart` + `lib/controllers/rider_tracking_controller.dart` (fases `_TrackPhase`) + `lib/widgets/tracking/tracking_map_view.dart` (fase arriving encuadra driver→pickup A PROPÓSITO — no "arreglarlo") + `lib/map/tracking_map_camera.dart` (chase por frame). **NUNCA una línea recta como ruta (2026-09-17):** el stopgap recto driver→pickup de arriving murió (`_updateApproachLine` es remove-only nativo+web — mientras la ruta vial vuela, la ruta del viaje atenuada + pins + carro en vivo cargan la pantalla); el Find-My (`rider_confirm_pickup_screen.dart`) NO dibuja NINGUNA línea — el mini mapa es dot DORADO (`_gold` + halo) + carro solos; la fetch de ruta sigue pero es `driving` y vive SOLO para `_snapToRoad` (el carro se pega a la calle si el fix cae ≤80 m de la ruta — "el carro en las líneas"), mientras "N ft"/flecha/detector FOUND miden al fix CRUDO del teléfono (`_lastDriverPos` — AMBAS rutas de cálculo la prefieren: el ticker de 300 ms y el handler GPS del rider sobre `driverPosOf()`, una sola fuente para que el número no se mueva impreciso), nunca al punto snapeado ni al "donde marcó llegada"; el dot del rider ES el fix crudo del propio teléfono (`_riderRaw` — el motor SmoothMotion quedó SOLO para el carro reléado; se sostiene solo si el fix reporta <0.6 m/s — nada de smoother 1 m detrás del rider caminando); flecha hero 280; y `_anchorRoutePoints` (directions_service.dart) ya no "rescata" geometría vacía como `[origin, destination]` — un provider con geometría vacía FALLA y cae al siguiente (getRoute filtra `points.isEmpty`). Guardián: `test/rider_find_my_guard_test.dart`.
- Encuadre onTrip (arreglado 2026-08-08): `_tripFramePoints()` usa `_tripRoutePts` = ruta COMPLETA pickup→dropoff, sembrada UNA vez en `_initRoute`. El traffic refresh (cada 2 min) y los reroutes solo reemplazan `_routePts` (pata restante, para dibujo/ETA) — JAMÁS `_tripRoutePts` o el frame colapsa a carro→dropoff (foto del zoom cerrado). Test guardián: `test/tracking_full_route_guard_test.dart`.
- Parpadeo de cámara cada ~2 s (arreglado 2026-08-09): la línea de ETA 2 min NO tenía histéresis → un semáforo flapeaba onTrip↔nearDestination en cada fix y el framer re-sembraba (flyTo 850 ms) EN CADA flip aunque ambas fases comparten el MISMO frame. Ahora: histéresis (entra ≤2, sale ≥4) + el reset es por KIND de frame (`_framedPhaseKind` 0=approach/1=trip), nunca por fase. Pineado en el mismo guardián.
- Cámara onTrip SIN auto-recentre (spec 2026-08-09): el fit es UNA sola vez por contenido (`_tripFitSignature` — ruta únicamente, JAMÁS `_animPos` o el carro re-dispara el fit) vía `fitBounds` (padding nativo → la ruta queda ENTRE la tarjeta de arriba y el sheet, nunca detrás). El per-frame `updateFollowFrame` solo corre en fase arriving. Drag libre; "Show route" (`_recenter`) re-encuadra al instante.

**Driver online / ofertas / viaje**
- `lib/screens/driver/driver_online_{screen,controller,map}.dart` — preview de oferta con fit a ruta (follow suprimido); tras aceptar, chase por frame zoom 17.5/pitch 55 = navegación turn-by-turn (intencional). `driver_trip_accept_screen.dart` — mini-mapa fit único (pickup+dropoff+ruta, SIN driverPos; zoom −0.2 clamp [9,15.5]; carrito = PNG del tracking rider `car_suv/sedan.png` iconSize 0.50, spec 2026-08-08); GPS solo mueve el carrito. Alto 400, rectángulo COMPLETO (cuadrado — el vignette radial de esquinas que lo ovalaba murió 2026-09-17) con fade SOLO borde (~3.5% lineal, transparente hacia adentro). Guardián: `test/driver_trip_minimap_guard_test.dart`.
- **Ofertas encadenadas (2026-08-09)**: el backend marca `chained: true` cuando el driver tiene viaje activo y la tarjeta vive EN el trip screen (`_onChainedOffers`/`_buildChainedOfferCard` en `driver_trip_accept_screen.dart` — SSE propio + poll 5 s de respaldo, NUNCA toca cámara/ruta). La reserva viaja en `lib/state/chained_ride_store.dart` (estático — la online screen se destruye en el pushAndRemoveUntil del rate screen). Terminar el viaje → rate screen `_startChainedTrip` (getTrip = solo liveness) abre el pickup del siguiente directo; cancelar el viaje actual → `DriverOnlineScreen.chainedHandoffOffer` → `_acceptOffer(alreadyAcceptedOnBackend: true)` / `_handoffChainedOffer` (TODA ruta de regreso a searching lo consume, incl. `_resetToSearchingOnRemoteCancel` — tanda 3). Accept fallido → el id va a `_chainedRejectedIds` o la tarjeta muerta resucita. Rechazar/expirar solo quita la tarjeta. Guardián: `test/driver_chained_offer_guard_test.dart`.
- **Tanda 3 (2026-08-09)**: PAUSE cierra AMBOS canales (SSE + poll) + gate `_isPaused` en `_applyOffers` (antes era decorativo). `_goOfflineBackend` NUNCA early-return por `_pos == null` (usa `last_driver_lat/lng` de LocalCache — sin esto el driver quedaba fantasma online). TODA ruta de regreso a searching llama `_startPosStream()` (el GPS se detiene al aceptar; accept fallido/cancel remoto lo dejaban muerto todo el turno). Rating rider: propinas solo sobre `widget.fare` real (sin fallback ficticio de $23; botones % ocultos sin fare) y `_submit` fallido muestra SnackBar y NO navega. Guardián: `test/driver_tanda3_guard_test.dart`.
- `lib/screens/driver/scheduled_rides_screen.dart` — tabs Requests/My Scheduled, diseño neu (`neuBox`/`neuBase` de `lib/widgets/neu_style.dart`, el sistema compartido — no sombras ad-hoc). Las tarjetas usan `StaticRoutePreview` (imagen estática; markers `pin-s-p+E8C547`/`pin-s-d+FFFFFF`, velo navy 0.30 — el pin blanco a velo 0.50 salía gris). Claim exitoso → `animateTo(1)` + reload de ambas listas (auto-move a My Scheduled). `scheduled_rides_marketplace_screen.dart` está MUERTA (sin referencias). Guardián: `test/driver_scheduled_neu_guard_test.dart`. **(2026-08-22)** el menú abre `scheduled_rides_map_screen.dart` (mapa + burbujas de precio + sheet deslizable + detalle con ruta y Reserve flotante; guardián `test/scheduled_rides_map_guard_test.dart`); la de tabs queda viva como "Your rides".
- **Handoff offline→online = snapshot probado con velo LIGERO (revert 2026-08-29):** el experimento de overlay transparente (DriverMapHost, la online adjunta al mapa vivo de home, 2026-08-25) FALLÓ en device — el mapa de home no renderiza bajo la ruta transparente en iOS (pantalla negra al GO tras login) y la UI de home sangraba a través del sheet de la online (GO + sheets dobles). Revertido: `_goOnline` vuelve a `_suspendMap()` + precache + push opaco, la online monta su propio MapWidget como siempre, y el hueco lo rellena `static_map_snapshot.dart` con **veilAlpha 0.30** (still nítido, no la niebla 0.85 de antes). La lección queda en AGENTS.md: PlatformView de Mapbox bajo ruta transparente NO es fiable en iOS — el "mapa único estilo Lyft" solo se logra fusionando la UI online DENTRO de home (sin push), lo cual queda como proyecto deliberado futuro, no como parche. Label "Finding trips" 15→17 pt y swap AnimatedSwitcher 100% secuencial (Interval 0.55).
- **Agendado reservado = compromiso estilo Lyft (2026-08-23):** `main.py _dispatch_reserved_scheduled_rides` corre ANTES del unclaimed en el dispatcher. Claim deja status `scheduled_accepted` (no `scheduled` + driver_id). A los 30 min: push recordatorio UNA vez (`trips.reminder_sent_at`, columna nueva — tras deploy correr `/admin/run-migrations`). En ventana ≤30 min con driver online → **asignación DIRECTA**: status `accepted` + `driver_assigned_at` + Firestore + push `driver_assigned` ("Tu reserva empieza ahora"), SIN DispatchOffer (la del claim ya quedó `accepted`). Driver offline → release: `driver_id=NULL`, status `scheduled`, offer del claim CANCELADA (o el guard "already accepted" bloquea la cascada), push rider `scheduled_driver_dropped`, y el unclaimed lo toma en el mismo tick. App: tap del push `driver_assigned` → `_handleDriverAssigned` (main.dart) abre DriverTripAcceptScreen; y si el driver está en "Finding trips", `_checkDirectlyAssignedTrip` (poll cada ~20 s, `GET /trips/active`) lo saca a la pantalla de viaje vía `_acceptOffer(alreadyAcceptedOnBackend: true)`. OJO: el reminder loop viejo también pushea a los 25-35 min — el driver recibe dos pushes distintos ~30 min (pendiente unificar). Guardián: `backend/tests/test_scheduled_reserved_flow.py`.
- **Navegación propia REVIVIDA + modernizada (2026-09-14, build 620):** `driver_nav_view.dart` vuelve a ser la navegación in-app del driver. **Continue, Directions (ambas piernas) Y Start Trip fase 1 (2026-09-17) abren `DriverNavView` por `_enterNavMode`** (entry único; libera el preview antes de montar — regla iOS una sola superficie; el mini mapa florece a pantalla completa vía `NavMorphOverlay` — bloom 700 ms easeInOutCubic + blur + feather + reveal cruzado 260 ms cuando el nav map reporta ready); `_exitNavMode` re-adquiere el preview (morph inverso); back sale de la nav, no del viaje. Las tarjetas de dirección conservan el chooser/Apple Maps (`_showNavigationSheet`/`_openAppleMaps`; el viejo `_openNativeMaps` murió con el cambio de Start Trip). La vista monta la superficie bajo owner por instancia (`DriverTripNav-N`), abre con la ruta prefetcheada `_routePoints` (gate 150 m al destino), chase 17.5/**25°** (bajado de 55° por spec 2026-09-17 — vista plana tipo referencia, calles legibles) con SmoothMotion + zoom dinámico (17.5/17.0 >20 m/s / 18.0 maniobra <150 m, lerp ≤0.5/s), barra de maniobra de `NavProgress` (directions_service.dart). **Off-route:** >40 m sostenido 4 s (reset 25 m), 1 fetch, cooldown 15 s, token `_rerouteSeq` anti-stale que se invalida al cambiar de destino; reemplazo atómico de la línea. **Estados:** `_CamState` (following/freeLook/recentering — gestos liberan la chase, Recenter flyTo 1.2 s) y `_NavPhase` (10 fases; leg-flip resetea llegada/fetch). Llegada interna <200 m approaching / 2 fixes <30 m+accuracy<25 m arrived (solo UI — los sliders del padre son la autoridad del flujo). Fallas: red caída conserva ruta (backoff 5/15/30) o routeError+retry; watchdog GPS 6 s con tarjeta de recuperación, PERO la tarjeta solo sale si la ubicación está REALMENTE apagada para la app (2026-09-17: `_diagnoseGpsStall` verifica `isLocationServiceEnabled()` + `checkPermission()` — un túnel/semáforo con permisos OK ya no la levanta). **Sheet (2026-09-17):** chat = disco oscuro, llamada = disco dorado relleno con glow (`_sheetCircleBtn(filled:)` — ambos wired a `_openChat`/`_call` del padre). **Velocímetro:** lee `pos.speed` y cae al `_dot.speedMps` del smoother cuando iOS reporta -1; deadband <0.45 m/s → 0 mph. **Deadband anti-twitch (2026-09-17):** parado/crawl (<1 m/s) la tangente de ruta solo re-apunta la flecha si cambia >20° — el fix GPS errático de un semáforo re-apuntaba la tangente cada segundo y la flecha giraba a tirones. **Pin destino PARADO (2026-09-17, "pin acostado"):** la flecha vive en su PROPIO `PointAnnotationManager` (`_carMgr`) — `icon-rotation-alignment: map` se aplica POR manager y estando en el compartido acostaba al teardrop/persona cada vez que la chase giraba; `_pointMgr` queda en viewport default → pin parado con la punta en la coordenada exacta (anchor BOTTOM). **Nav = SOLO navegación (2026-09-17):** no hay botón Arrived ni sliders de recoger/terminar dentro de la nav — al llegar, la zona del sheet muestra SOLO **End Route** (`_endRouteVisible` = latch interno o stage post-llegada del padre) que devuelve al driver a la trip page, cuyos botones/sliders son la autoridad del flujo (`_buildStageControl`/`_buildSlideBar`/`_onSlideUpdate` borrados; los callbacks `onArrived`/`onSlidePickUp`/`onSlideFinish` quedan en la API del widget sin uso por si acaso). **Rider en vivo = dot azul (2026-09-17):** la ubicación del rider en la pierna de pickup se dibuja como el puck azul (`_riderHaloAnnot` halo 13 @0.22 + `_riderDotAnnot` 7 `_riderBlue` 0xFF3B82F6 anillo blanco, CircleAnnotations nativos) — el ícono de persona dorado murió (`_renderRiderFigure` borrado). Guardián: `test/driver_nav_guard_test.dart` (31 pines). Historia: introducida 2026-08-22, archivada 2026-08-23 (Start Trip a mapas externos), revivida 2026-09-14; Start Trip fase 1 volvió a in-app 2026-09-17. El mini-mapa del trip screen sigue VIVO cuando no hay nav (carrito con GPS + ruta borrándose).
- **GPS background = solo ONLINE (2026-08-22):** `driverLocationSettings(background:)` — `false` quita `allowBackgroundLocationUpdates` (iOS) y el ForegroundNotificationConfig (Android). Home corre su stream foreground-only cuando `_isStillOnline == false` y lo RE-CREA en cada flip (`_syncPosStreamMode` — iOS solo aplica el flag en un request nuevo); online sigue con background. La píldora azul con el driver offline era home pidiendo background siempre. **La píldora azul NO se muestra NUNCA** (`showBackgroundLocationIndicator: false` siempre) — estando online el indicador del turno es la Live Activity (ícono Cruise en la isla / tarjeta en lock screen), no la barra azul de GPS.
- **Multi-vehículo (2026-08-29/30):** cada carro carga SUS docs — insurance/registration (+ `vehicle_inspection` solo AL, pineado en `test_onboarding_items.py`) llevan `vehicle_id`; docs driver-level quedan NULL. Upload sin vehicle_id cae al vehículo activo/más reciente (`_resolve_doc_vehicle` en drivers.py — el onboarding nunca tuvo uno que mandar). `can_go_online` endurecido (2026-08-30): exige cuenta aprobada + carro ACTIVO aprobado + sus 3 docs vivos — antes `no_vehicle`/`vehicle_not_approved` eran advisory y un doc NULL valía para TODOS los carros (el carro #2 heredaba la inspección del #1). Legacy NULL / columnas `users.{insurance,vehicle_registration,inspection}_url` cuentan SOLO con un vehículo en cuenta. Un carro solitario `pending` bajo cuenta aprobada = revisado (NADA estampa `approval_status`: ni el panel ni el webhook Checkr — `POST /admin/vehicles/{id}/status` existe pero SIN UI en este repo; el panel de dispatch vive fuera). Backfill de docs NULL → carro activo: `backend/scripts/backfill_vehicle_doc_ids.py` (idempotente, railway run). Guardián: `backend/tests/test_can_go_online_vehicles.py`.

**Registro driver — documentos + biometría (Issues 3 y 5, arreglados 2026-08-07)**
- `lib/screens/driver/driver_signup_screen.dart` (3 pasos). Licencia Front/Back → `license_guidelines_screen.dart` (página de guías) → `license_scanner_screen.dart` (cámara + OCR). Seguro/registro de auto: `_showPickOptions` (cámara/galería).
- Cara 4 pasos: `lib/screens/face_liveness_screen.dart` (minFaceSize 0.1, feedback visible, errores en pantalla) + `lib/utils/face_oval_fit.dart` (math pineada por `test/face_oval_fit_test.dart` — no cambiar umbrales). iOS (2026-08-08): la connection rota los buffers NATIVAMENTE → `_rotationDegrees()` devuelve 0 en iOS (el sensor angle doble-rota y ML Kit ve la cara acostada) y el frame mostrado se deriva del primer frame del stream vía `displayedFrameSize()` (previewSize de iOS reporta el formato del sensor, NO la textura — usarlo pelado zoomea ×1.33). Log `[FaceFit]` 1/s.
- Scanners de documentos (rider KYC y `license_scanner_screen.dart` driver): preset vía `_capturePreset` — Android `ResolutionPreset.max` (`high` da stills 720p y el crop del marco queda ~485×306 px → OCR vacío → rechazos `ocr_unreadable`), **iOS `ultraHigh` NUNCA `max`** (2026-08-09: el branch `.max` del plugin pone `isHighResolutionPhotoEnabled` en el still → NSException nativa en iOS 16+ y la app SE CIERRA en la captura del still; 4K deja el crop ~1940×1224, sobrado para OCR). `test/doc_frame_crop_test.dart` ya pinea stills 3024×4032. Guardián: `test/camera_kyc_guard_test.dart`. **Detección OCR = image stream SILENCIOSO (2026-08-30):** `takePicture()` por tick sonaba el shutter cada 1.5 s (0.55 s en card scan) — la página "tomaba fotos sola". Los 3 scanners (KYC inline, licencia driver, `card_scan_screen.dart`) escanean frames del stream (nv21 Android/bgra8888 iOS, rotación 0 en iOS como face_liveness) con throttle + guard `_scanning`; el `takePicture` real va solo en el tap del usuario, tras `stopImageStream()`, y `_retake`/cambio frente-dorso reinicia el stream.
- Guías compartidas: `lib/widgets/doc_guidelines_view.dart` — UNA sola fuente del diseño; la usa también el KYC rider.

**Identidad rider (KYC + auto-verificación por nombre, 2026-08-08)**
- `lib/screens/identity_verification_screen.dart` — paso 7 = guías (`DocGuidelinesView`), scanner inline con OCR + crop (`lib/utils/doc_frame_crop.dart`, pineado por `test/doc_frame_crop_test.dart`). El OCR COMPLETO del frente del ID viaja como `id_ocr_text` en `submitVerification` (el dorso no — es ruido de barcode). **El OCR se lee del STILL COMPLETO primero y del crop SOLO como fallback (2026-08-09)** — leerlo solo del crop dejaba el texto vacío en devices cuyo crop mapea mal (rotación/EXIF) y el backend rechazaba `ocr_unreadable` con el ID legible (prod: rider 69).
- Flujo: submit → rider queda `pending` → `_auto_verify_rider` en `backend/routers/auth.py` espera 10-15 s y aprueba automáticamente a TODOS los riders (no verifica OCR ni nombre). La decisión admin/dispatch NO se pisa. `verification_reason` `name_mismatch` / `ocr_unreadable` quedaron obsoletos. Guardianes: `backend/tests/test_rider_name_verify.py`, `test_rider_autoapprove.py`.
- Gate UI: hero "Where to?" (hoy card "Ride / Schedule") SIEMPRE se ve normal (rama `verificationBlocked` eliminada); ambos taps gatean con `_ensureVerified()` en `_buildHeroCTA`/`_openScheduleSheet` → abren el KYC. Server-side: `POST /trips` 403 (trips.py:336).

**Flujo rider booking/schedule NUEVO (2026-08-22, estilo Lyft):** home (dock fijo fuera del scroll, logo Cruise en tab Ride, sin pill dorada de activo, sin Priority/Schedule/10%off ni campana/settings/handle) → hub `schedule_hub_screen.dart` (Plan ahead + Your calendars con device_calendar_plus — el paquete `device_calendar` literal está abandonado y pinea timezone viejo) → direcciones `pickup_dropoff_search_screen.dart` (X, título dinámico Schedule a ride/Destination/Start, sin Change rider, "+" stop) → aerolíneas `airline_select_screen.dart` si dropoff es aeropuerto → ruedas Depart/Arrive `schedule_datetime_screen.dart` (estimate real) → choose ride (botón "Select {tier}", Apple Pay default en iOS) → `set_pickup_location_screen.dart` (pin fijo fluido, puntos sugeridos via queryRenderedFeatures en clases de calle, snap ≤25 m con morph a "Recommended", nota 60 chars con chips) → hold existente → pipeline. `waiting_for_driver_screen.dart` NO se empuja tras pagar (decisión de producto documentada en ride_request_controller ~L1544: doble pantalla; el pipeline searching la cubre). Flujo viejo BORRADO (schedule_picker_sheet/schedule_ride_flow/schedule_booking_screen/scheduled_rides_screen rider). Fee de cancelación de agendados por tier (compact $10/standard $15/premium $25/black $35, cap upfront, gratis >60 min o sin driver): constantes `SCHEDULED_CANCEL_*` en trips.py espejadas en `schedule_cancel_policy_screen.dart` (guard `test/schedule_hub_guard_test.dart` compara ambas fuentes; backend `test_scheduled_cancel_fee.py`). Permisos rider: secuencia ubicación→notificaciones tras primer login (flag `rider_perms_prompted_v1`); cold start con sesión sin permisos → `rider_permissions_screen.dart` (una vez por proceso). Guardianes: rider_home_dock/rider_booking_flow/schedule_hub + los viejos.
- Tiempo real cuenta: `SocketService.accountStatusStream` escucha `account_status_changed` (blocked/deleted → logout, deactivated → `AccountDeactivatedScreen`, approved → refresh); poll de 300 s queda de respaldo. `AccountDeactivatedScreen` tiene FAB dorado de chat soporte.
- Guardián Flutter: `test/rider_verification_guard_test.dart`.

**Motor de mapas**
- `lib/map/`: `unified_map_service`, `camera_director` (locks exclusivos), `tracking_map_camera`, `map_surface_coordinator` (una sola superficie viva). Web = Mapbox GL JS en `web_map_view_web.dart`. SDK `mapbox_maps_flutter ^2.5.0`.

**Trip lifecycle backend**
- `backend/routers/trips.py`: statuses canónicos + `_STATUS_ALIASES` + `_VALID_TRANSITIONS` (forward-only), comisiones `_COMMISSION_BY_TYPE`. `Trip.distance` = MILLAS, `Trip.duration` = MINUTOS; ambos NULL hasta que el viaje completa. La rama **resurrect** (driver continúa un viaje auto-cancelado, cancelled/completed→arrived/in_trip/completed) usa `request.client.host` — `update_trip_status` DEBE declarar `request: Request` o es NameError 500 siempre (bug 2026-08-09, pineado en `backend/tests/test_trip_resurrect_guard.py`).

**Referidos (2026-08-16, rediseñado)**
- **Riders (Cruise Cash):** `backend/routers/referrals.py` — política centralizada en constantes: 2 viajes del referido de $25+ → **$25 a cada lado** (antes el amigo nunca recibía nada). `credit_referrer_if_qualified` (hook en trips.py al completar) paga AMBOS + push. Códigos: nombre hasta 6 letras + sufijo sin look-alikes (0/O/1/I/L); redeem/transfer aceptan cualquier formato (`_normalize_referral_code`). `/referrals/me` expone `policy` + `my_pending_bonus` (banner del referido). Share message lleva `https://cruiseinride.com/download`. Pantalla: `lib/screens/referral_screen.dart` (sin hero; banner verde "tienes $ esperándote"; i18n ES/EN). Guardián: `backend/tests/test_referral_double_payout.py`.
- **Drivers (cash):** `backend/routers/driver_referrals.py` — HITOS: 2 viajes del referido → **$25 al driver nuevo**; 50 viajes/60 d → **$50 al referidor**; 200 viajes/180 d → **$150 más**. Status: pending → milestone1 → qualified | expired (pasar la ventana de 60 d sin los 50 viajes expira TODO). Config por AppConfig (`driver_referral_m1_*`, `m2_*`, `referee_bonus_*`). Filas legacy (rides_required==50) conservan su $200 flat. Columna nueva `driver_referrals.referee_bonus_paid_at` (en boot migration list de database.py). `_aware()` normaliza datetimes naive de SQLite. Guardián: `backend/tests/test_driver_referral_milestones.py`.
- **Códigos al signup:** `auth.register` mintea `referral_code` (todos) y `driver_referral_code` (drivers) — antes era lazy al abrir la pantalla y las transferencias a quien nunca la abrió fallaban "Recipient not found".
- App driver screen (`driver_referral_screen.dart`) lee las llaves legacy del settings payload (totales) — funciona, pero NO muestra hitos todavía (pendiente UI).

**Reopen flow (tanda 1 auditoría, 2026-08-09)**
- Pantallas de rating NUNCA montan MapWidget: el backdrop es `StaticRoutePreview(pins: false)` (imagen) — un mapa nativo ahí vive junto al de tracking durante la transición = 2 superficies = crash iOS al final de CADA viaje. Guardián: `test/reopen_flow_guard_test.dart`.
- El auto-resume de viaje activo en home es POR PROCESO (`HomeScreen.autoResumeConsumed` estático): salir de tracking con back lo setea ANTES de construir home (`_navigateToHome`) — con latch por instancia, home re-abría tracking siempre (rebote infinito).
- `_getPageForRoute` (main.dart) responde TODA ruta nombrada con `SplashScreen` — NUNCA `pushNamedAndRemoveUntil('/home')` (re-corre el boot entero); navegar con `const HomeScreen()` directo.

**Pagos / holds (2026-08-08)**
- Hold OBLIGATORIO del total estimado (capture_method=manual) antes de crear el viaje — inmediato (`/dispatch/request`) y agendado (`POST /trips`): sin PI válido (`requires_capture`) → 402 y no se crea nada (producción; testers/sandbox/`TEST_MODE_RIDER_IDS` con bypass).
- `increment_authorization` extiende el hold cuando algo lo supera: surcharges al crear (dispatch.py), wait time al pasar a `in_trip` (trips.py ~L1445). Emisor sin soporte → warning + shortfall de `_charge_trip` de respaldo (no bloquea).
- Agendados: el dispatcher (`main.py _scheduled_ride_dispatcher`) re-verifica el hold antes de despachar; expirado → re-autoriza off-session; declined → cancela SIN fee (`payment_declined`) + push.
- Hold NO liberable por el rider: `payments.cancel/capture` dan 409 con viaje activo; la liberación solo por `_release_or_capture_fee_on_cancel` (fee $5 si driver en ruta >2 min). **TODA ruta que cancela con hold vivo pasa por ese helper** (tanda 2, 2026-08-09): guardian 10min on-demand / 30min agendados / 180min ghost — antes usaban `Refund.create` sobre un PI `requires_capture` (falla, hold clavado 7 días) o no tocaban el hold; agendados además pushea `scheduled_cancelled` al rider. Webhook `payment_failed` JAMÁS pisa un trip ya `paid` (shortfall post-cobro lo volteaba). `PaymentMonitor` barre `held` (escribía `authorized` = código muerto). `DispatchTimeoutAgent` al expirar una oferta manual (scheduler agendados / retry agent) devuelve el trip a `driver_id=NULL` para que UnmatchedTripRetry lo re-despache — antes quedaba huérfano en `requested` para siempre. Guardián: `backend/tests/test_hold_settlement_guard.py`.
- Split al completar: 70/30 flat todas las tiers; driver cobra por Connect (payout semanal lunes 00:00 America/Chicago, `_next_payout_run` dinámico con DST / instant). ACH no soporta holds (documentado). Guardianes: `backend/tests/test_hold_total.py`, `test/ride_hold_guard_test.dart`.
- **Regla dinero-real (2026-08-17):** NADA acredita al driver sin cobro real. TODO split pasa por `_credit_driver_earnings` (trips.py ~L227) — gate `status=='completed' AND payment_status=='paid'`, idempotente vía `driver_earnings IS NULL`, SIN propinas (las paga el tip endpoint solo si su cargo sale `succeeded`). Se llama: tras el auto-cobro de `update_trip_status`, en el cobro manual `/trips/{id}/charge`, en ambos webhooks `payment_intent.succeeded` (webhooks.py + payments.py — ACH "processing" acredita ahí) y en admin PATCH `/admin/trips` (ya NO tiene pipeline propio). Cancel fee: `_credit_driver_cancellation_fee` solo acredita si `_release_or_capture_fee_on_cancel` dejó `payment_status='paid'`. Viajes Test Mode (`TEST_MODE_RIDER_IDS`, sin PI) y cargos fallidos ya NO pagan. Guardián: `backend/tests/test_earnings_real_money_guard.py`. Limpieza de data histórica: `backend/scripts/cleanup_test_earnings.sql`.

---

## 🪤 Trampas y bugs silenciosos conocidos

0. **Columna ORM nueva ≠ columna en prod** (outage 2026-08-08): agregar una Column al modelo + a las listas ensure-column NO garantiza que el boot migration la cree — cada query de la tabla entera falla con `UndefinedColumn` y envenena la transacción (500s en cadena, `InFailedSqlTransaction`). Remedio inmediato sin redeploy: `railway run bash -c 'curl -s -X POST -H "x-api-key: $API_KEY" https://cruiseapp2-production.up.railway.app/admin/run-migrations'`. Verificación: logs sin `UndefinedColumn` fresco. Tras cualquier deploy con columna nueva, chequear los logs por "Added column" / "migration skip".

1. **FCM `priority="max"` va en palabra pelada** — el SDK la prefija con `PRIORITY_`; pasar el nombre de wire mata el Message ENTERO en silencio (pasó dos veces). Hay test guardián.
2. **El bloque `notification` del FCM SE QUEDA** — sin él Android no dispara `onMessageOpenedApp`/`getInitialMessage` y el tap no hace nada. El dedup vive en el cliente (bg handler retorna temprano en ofertas).
3. **Sonido APNs custom:** el `.wav` debe ser miembro del target Runner en `project.pbxproj` (lo es desde 2026-08-07). Si falta, iOS cae al sonido default SIN error ni log.
4. **`Runner.entitlements` lleva `aps-environment=production`** (2026-08-07) — exige la capability Push Notifications en el App ID de App Store Connect o la firma falla.
5. **TODO build (iOS y Android) se compila por Codemagic** — no hay OTA. Cambios nativos (pbxproj, entitlements, AndroidManifest, pods) y de Dart requieren build completo por Codemagic.
6. **`pickerMode` queda `true` tras un Confirm exitoso** — jamás gates con `widget.pickerMode`; gatea por fase `pickingLocation`.
7. **Checklist anti-bug-silencioso (aplicar a TODO fix):** (a) ¿el archivo/asset es miembro del bundle/target nativo? (b) ¿el error llega a la UI o solo a `debugPrint`/`catch (_) {}`? (c) ¿todo gate de fase/estado tiene su camino de salida explícito? (d) ¿el push lleva contenido visible o solo ids? (e) ¿hay entradas HERMANAS al mismo flujo (front/back, cámara/galería, Android/iOS) que necesitan el mismo cambio? — grep por hermanos, no solo el caso reportado.
8. **`face_liveness_screen_new.dart` fue borrado** (duplicado muerto con el bug yuv420 + catch silencioso) — no recrear ni re-importar.
9. **Tests que ya fallaban en HEAD (no son tuyos, no los persigas):** `test_dispatch_radius.py::test_every_live_path_uses_the_shared_ceiling`, `test_rating_thresholds.py` (3), `test_state_filter_all_paths.py::test_live_dispatch_has_no_state_filter`. Flutter: `crashlytics_guards_test.dart` FIX 2 (el sanitize quedó DESPUÉS del primer `io.io(` en el fuente — el índice que pinea ya venía invertido) y `ride_hold_guard_test.dart` (c) (flake de orden: aislado pasa, en corrida completa falla). (`test_rider_terms_compliance.py::test_wait_fee_schedule_consistent_between_ui_and_backend` ya PASA desde 2026-09-13 — fue reescrito para la política no-show con mínimo por tier.)
10. **Las reglas Firebase del repo NO son las desplegadas** — nunca `firebase deploy` de rules sin comparar con la consola (detalle: CLAUDE.md #27-29).
11. **`flutter_stripe` y Mapbox no corren en web** (crash en primer layout) — todo flujo con ellos va tras `kIsWeb`; por eso las pantallas de mapa/pagos no se verifican en `localhost`.
12. **Cuando el usuario reporta un bug visual, CRÉELE PRIMERO y rastrea el layout — no culpes al build.** (2026-08-28: el strip de mapa bajo el sheet de tiers lo descarté dos veces como "build viejo" porque `bottom: 0` "debía" ser flush — y era real: el `SafeArea` estaba FUERA del contenedor que pinta el fondo, dejando la franja del home-indicator transparente.) Regla: si el usuario lo ve en el build actual, existe; lee la jerarquía de widgets completa antes de argumentar versión.
13. **El inset del sistema lo pinta el fondo del sheet, nunca un SafeArea transparente por fuera** — un `SafeArea`/`Padding` que envuelve al contenedor con color deja una franja transparente por donde se ve el mapa. El inset va DENTRO del padding del contenedor que tiene el color (fix en `ride_request_widgets.dart` `_buildRoutePreviewSheet`, 2026-08-28).
14. **Cada push de feature nueva necesita su bump de build en `pubspec.yaml` el mismo día** — Codemagic no adivina; App Store Connect rechaza versiones duplicadas y un build viejo en el device del usuario produce reportes "fantasma" de bugs ya arreglados (o esconde los reales, ver #12).
15. **`railway.toml` es load-bearing (probado 2026-08-29)** — el dashboard del servicio NO tiene el Dockerfile path configurado; borrar el archivo hace que Railway caiga a Railpack (auto-detección Node) y el build muere con "No start command detected" (deploy e07e4191 falló así; el servicio quedó online con el deploy anterior y se restauró el archivo).
16. **El tier de rate limit por IP (100/min) NO cuenta los polls GET de solo lectura** (2026-08-30, outage trip 625: el rider con viaje activo + support chat genera 100+ req/min de polling y el 429 tumbaba cancel/chat) — `_is_read_poll_path` en `main.py` exime `/trips/active`, `/trips/{id}/poll`, `/trips/{id}/chat`, `/support/chats[/.../messages]` (siguen bajo el cap DDoS 3000; los POST conservan el tier). Al meter un endpoint polleable nuevo, clasifícalo ahí. Guardián: `backend/tests/test_rate_limit_read_polls.py`.
17. **Mezclar `time.time()` con `time.monotonic()` = comparación siempre falsa/verdadera** (2026-09-17, flap offline/online en prod): `drivers.py` estampa `_driver_locations[...]["ts"]` con `time.monotonic()` (el reloj que TTLCache usa internamente) y `DriverLocationStaleDetector` (guardian_agent.py) lo comparaba contra `time.time()` (época ~1.7e9) → edad enorme siempre → marcaba offline a TODOS los drivers online cada 2 min aunque el GPS llegara cada segundo; el heartbeat los re-marcaba online → ofertas suprimidas en la ventana offline y estado GPS "inestable". Regla: TODO timestamp guardado en cachés in-memory va en `time.monotonic()` y se compara con `time.monotonic()`. Guardián: `backend/tests/test_driver_location_stale_detector.py`.

---

## 🚦 Edit safety

**Nunca editar sin aprobación explícita:** `pubspec.yaml`, `codemagic.yaml`, `railway.toml`, `firebase.json`, `.firebaserc`, `database.rules.json`, `firestore.rules`, `storage.rules`, `android/app/build.gradle.kts`, signing en `ios/Runner.xcodeproj/` (agregar RECURSOS como sonidos sí está permitido si el fix lo requiere — verificado 2026-08-07), `backend/models/database.py` (sin plan de migración), `backend/migrations/*.py` ya corridas, `backend/migrate.py`, `backend/config.py`, `backend/utils/security.py` (sin tests de regresión), docs legales en `docs/` (privacy/ToS/agreements), `.github/copilot-instructions.md`, `.github/agents/*.md`, `.github/workflows/*.yml`, `CLAUDE.md`.

**Siempre:** `flutter analyze` tras editar `.dart`; `pytest` tras editar Python; `try/finally` + `.dispose()` en controllers; Pydantic en inputs FastAPI; SQLAlchemy ORM o `text()` con parámetros nombrados; strings user-facing vía `S.of(context).xxx` (bilingüe ES/EN); `maybeOf` fuera de `build()` (CLAUDE.md #26); nunca commitear secretos.

## 🔐 Seguridad

- `backend/services/redis_cache.py` ya NO usa `pickle` — solo valores JSON-serializables.
- CORS en `backend/main.py` es production-safe; localhost solo con `DEBUG=1`.
- Hay secretos hardcodeados históricos en assets móviles/web (Firebase keys, Stripe pk_live, Google Maps key) — no agregar nuevos; plan de rotación en `SECURITY_ACTIONS.md`.

## 🛠️ Comandos

```bash
flutter analyze                                            # tras editar Dart
cd backend && ./.venv/Scripts/python.exe -m pytest tests/ -q   # tras editar Python
railway up --detach                                        # deploy backend
# iOS y Android = Codemagic — NUNCA flutter build apk manual (no hay OTA)
```

*Última actualización: 2026-08-07 — convertido en cerebro de navegación Kimi tras la sesión de 5 fixes (push ofertas, snaps de mapa, guías licencia, cara).*
