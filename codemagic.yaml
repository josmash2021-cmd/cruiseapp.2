workflows:
  ios-sideload:
    name: iOS IPA para Sideloadly (sin Developer Account)
    max_build_duration: 60
    instance_type: mac_mini_m2
    environment:
      flutter: stable
      xcode: latest
      cocoapods: default
      vars:
        FLUTTER_XCODE_CODE_SIGNING_ALLOWED: "NO"
        FLUTTER_XCODE_CODE_SIGNING_REQUIRED: "NO"
        ENV_API_KEY: "HWB88VurhLM-1GdVML2PT92iqNSbeJ52TU1VO37MBZS6RYlyWvfIpaTdD54GT_5u"
        ENV_HMAC_SECRET: "qUDmTNu1Dxxg_xo7kaUfRba4XiU_5H1ZhkUMDuVrD2dLQ2ImT8JXZ5FgUyXpSJ5h"
        ENV_GOOGLE_MAPS_KEY: "AIzaSyALnqq4-_jJLUCLxSJaWZGZHgw27RVE78Y"
        ENV_EMAILJS_SERVICE_ID: "service_kgjbuew"
        ENV_EMAILJS_TEMPLATE_ID: "template_oucb3n9"
        ENV_EMAILJS_PUBLIC_KEY: "5R65y1qr1-lXDwGRb"
        ENV_EMAILJS_PRIVATE_KEY: "xeR8WDCTgskv9g9ITzote"
        ENV_STRIPE_PK: "pk_test_REPLACE_WITH_YOUR_KEY"
        ENV_STRIPE_MERCHANT: "merchant.com.cruise.app"
    scripts:
      - name: Generate env.dart
        script: |
          cat > lib/config/env.dart << ENVEOF
          class Env {
            static const String apiKey = '${ENV_API_KEY}';
            static const String hmacSecret = '${ENV_HMAC_SECRET}';
            static const String googleMapsKey = '${ENV_GOOGLE_MAPS_KEY}';
            static const String emailjsServiceId = '${ENV_EMAILJS_SERVICE_ID}';
            static const String emailjsTemplateId = '${ENV_EMAILJS_TEMPLATE_ID}';
            static const String emailjsPublicKey = '${ENV_EMAILJS_PUBLIC_KEY}';
            static const String emailjsPrivateKey = '${ENV_EMAILJS_PRIVATE_KEY}';
            static const String stripePublishableKey = '${ENV_STRIPE_PK}';
            static const String stripeMerchantId = '${ENV_STRIPE_MERCHANT}';
          }
          ENVEOF
          echo "env.dart generated:"
          cat lib/config/env.dart
      - name: Get Flutter packages
        script: |
          flutter pub get
      - name: Generate iOS config files
        script: |
          flutter build ios --config-only --no-codesign --no-pub
      - name: Install CocoaPods dependencies
        script: |
          cd ios
          for i in 1 2 3; do
            echo "=== pod install attempt $i ==="
            pod install && break
            echo "Attempt $i failed, waiting 30s..."
            sleep 30
          done
          if [ ! -d Pods ]; then
            echo "ERROR: Pods directory not found after 3 attempts"
            exit 1
          fi
          echo "CocoaPods installed successfully"
      - name: Disable code signing in project
        script: |
          cd ios
          python3 << 'PYEOF'
          import re, glob

          def patch_pbxproj(path):
              with open(path, 'r') as f:
                  content = f.read()
              # Remove entitlements
              content = re.sub(r'\s*CODE_SIGN_ENTITLEMENTS = [^;]*;', '', content)
              # Clear signing identity
              content = re.sub(r'"CODE_SIGN_IDENTITY\[sdk=iphoneos\*\]" = "[^"]*"',
                               '"CODE_SIGN_IDENTITY[sdk=iphoneos*]" = ""', content)
              content = re.sub(r'CODE_SIGN_IDENTITY = "[^"]*"',
                               'CODE_SIGN_IDENTITY = ""', content)
              # Remove development team
              content = re.sub(r'DEVELOPMENT_TEAM = [^;]*;',
                               'DEVELOPMENT_TEAM = "";', content)
              # Set manual signing
              content = re.sub(r'CODE_SIGN_STYLE = Automatic',
                               'CODE_SIGN_STYLE = Manual', content)
              # Disable product validation
              content = re.sub(r'VALIDATE_PRODUCT = YES',
                               'VALIDATE_PRODUCT = NO', content)
              # Remove existing CODE_SIGNING_ALLOWED/REQUIRED lines to avoid dupes
              content = re.sub(r'\n\s*CODE_SIGNING_ALLOWED = [^;]*;', '', content)
              content = re.sub(r'\n\s*CODE_SIGNING_REQUIRED = [^;]*;', '', content)
              # Inject CODE_SIGNING_ALLOWED=NO into every buildSettings block
              content = content.replace(
                  'buildSettings = {',
                  'buildSettings = {\n\t\t\t\tCODE_SIGNING_ALLOWED = NO;\n\t\t\t\tCODE_SIGNING_REQUIRED = NO;'
              )
              with open(path, 'w') as f:
                  f.write(content)
              print(f"Patched: {path}")

          patch_pbxproj('Runner.xcodeproj/project.pbxproj')
          for p in glob.glob('Pods/**/*.pbxproj', recursive=True):
              patch_pbxproj(p)
          PYEOF

          # Inject into xcconfig (no leading spaces)
          printf 'CODE_SIGNING_ALLOWED=NO\nCODE_SIGNING_REQUIRED=NO\nCODE_SIGN_IDENTITY=\nCODE_SIGN_ENTITLEMENTS=\nEXPANDED_CODE_SIGN_IDENTITY=\n' >> Flutter/Release.xcconfig
      - name: Flutter build iOS (no codesign)
        script: |
          # Disable tree-shake-icons via xcconfig (non-constant IconData in project)
          echo 'TREE_SHAKE_ICONS=false' >> ios/Flutter/Release.xcconfig

          # Use xcodebuild DIRECTLY to bypass Flutter's Development Team validation.
          # flutter build ios --no-codesign still checks for a team and fails;
          # calling xcodebuild ourselves avoids that Flutter-level check entirely.
          xcodebuild build \
            -workspace ios/Runner.xcworkspace \
            -scheme Runner \
            -configuration Release \
            -sdk iphoneos \
            -destination 'generic/platform=iOS' \
            BUILD_DIR="$(pwd)/build/ios" \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGN_ENTITLEMENTS="" \
            DEVELOPMENT_TEAM="" \
            PROVISIONING_PROFILE_SPECIFIER="" \
            CODE_SIGN_STYLE=Manual \
            VALIDATE_PRODUCT=NO \
            COMPILER_INDEX_STORE_ENABLE=NO

          # Find the .app — xcodebuild puts it in Release-iphoneos/
          APP=$(find build/ios/Release-iphoneos -name "*.app" -type d 2>/dev/null | head -1)
          if [ -z "$APP" ]; then
            APP=$(find build/ios -name "*.app" -type d 2>/dev/null | head -1)
          fi

          if [ -z "$APP" ]; then
            echo "ERROR: No .app bundle found anywhere in build/ios/"
            find build/ -type d -name "*.app" 2>/dev/null
            exit 1
          fi

          # Verify .app has real content
          APP_SIZE=$(du -sm "$APP" | cut -f1)
          echo "Build produced: $APP ($APP_SIZE MB)"

          if [ "$APP_SIZE" -lt 2 ]; then
            echo "WARNING: .app is small ($APP_SIZE MB), checking contents..."
            ls -la "$APP/"
            echo "ERROR: .app appears empty or corrupt"
            exit 1
          fi

          echo "Build OK - .app ready for packaging"
      - name: Empaquetar .app en .ipa
        script: |
          # Search for .app in Release-iphoneos first, then fallback
          APP_PATH=$(find build/ios/Release-iphoneos -name "*.app" -type d 2>/dev/null | head -1)
          if [ -z "$APP_PATH" ]; then
            APP_PATH=$(find build/ios -name "*.app" -type d 2>/dev/null | head -1)
          fi

          if [ -z "$APP_PATH" ]; then
            echo "ERROR: No .app found to package"
            exit 1
          fi
          echo "Packaging: $APP_PATH"

          # Clean previous packaging artifacts
          rm -rf build/ios/ipa
          mkdir -p build/ios/ipa/Payload
          cp -r "$APP_PATH" build/ios/ipa/Payload/

          cd build/ios/ipa

          # Verify Payload has content
          echo "Payload contents:"
          ls -la Payload/
          du -sh Payload/

          zip -r cruise-app.ipa Payload

          # Show final IPA size
          IPA_SIZE_BYTES=$(stat -f%z cruise-app.ipa 2>/dev/null || stat -c%s cruise-app.ipa 2>/dev/null)
          IPA_SIZE_MB=$(du -sm cruise-app.ipa | cut -f1)
          echo "IPA created: cruise-app.ipa ($IPA_SIZE_BYTES bytes / $IPA_SIZE_MB MB)"

          if [ "$IPA_SIZE_BYTES" -lt 50000 ]; then
            echo "ERROR: IPA too small ($IPA_SIZE_BYTES bytes) - something went wrong"
            exit 1
          fi

          echo "IPA packaging completed successfully"
    artifacts:
      - build/ios/ipa/cruise-app.ipa
      - /tmp/xcodebuild_logs/*.log

  ios-adhoc:
    name: iOS Ad Hoc (instalar en iPhone)
    max_build_duration: 120
    instance_type: mac_mini_m2
    integrations:
      app_store_connect: Codemagic
    environment:
      ios_signing:
        distribution_type: ad_hoc
        bundle_identifier: com.cruiseapp.cruise
      flutter: stable
      xcode: latest
      cocoapods: default
    scripts:
      - name: Get Flutter packages
        script: |
          flutter pub get
      - name: Generate iOS config files
        script: |
          flutter build ios --config-only --no-codesign
      - name: Install CocoaPods dependencies
        script: |
          cd ios && pod install
      - name: Flutter build IPA (ad hoc)
        script: |
          flutter build ipa --release --no-tree-shake-icons
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
    publishing:
      email:
        recipients:
          - josmash2021@gmail.com
        notify:
          success: true
          failure: true

  ios-release:
    name: iOS Release Build (App Store)
    max_build_duration: 120
    instance_type: mac_mini_m2
    integrations:
      app_store_connect: Codemagic
    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.cruiseapp.cruise
      flutter: stable
      xcode: latest
      cocoapods: default
    scripts:
      - name: Get Flutter packages
        script: |
          flutter pub get
      - name: Generate iOS config files
        script: |
          flutter build ios --config-only --no-codesign
      - name: Install CocoaPods dependencies
        script: |
          cd ios && pod install
      - name: Flutter build IPA (App Store)
        script: |
          flutter build ipa --release --no-tree-shake-icons
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
    publishing:
      email:
        recipients:
          - josmash2021@gmail.com
        notify:
          success: true
          failure: true