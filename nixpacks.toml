[phases.setup]
nixPkgs = ["postgresql"]  # For pg_dump used by db_backup.py

[phases.install]
cmds = [
    "pip install -r backend/requirements.txt"
]

[phases.build]
cmds = [
    "echo 'Build phase complete'"
]

[start]
cmd = "cd backend && bash start.sh"
